version: '3.8'

services:
  app:
    build: .
    ports:
      - "80:80"
      - "3001:3001"
    environment:
      - DATABASE_URL=${DATABASE_URL}
    volumes:
      - ./apps/frontend/dist:/app/apps/frontend/dist:ro
    restart: unless-stopped
