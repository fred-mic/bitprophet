version: '3.8'

services:
  app:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
      - "3001:3001"
    environment:
      - DATABASE_URL=${DATABASE_URL}
    restart: unless-stopped
