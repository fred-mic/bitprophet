{
  "name": "my-bun-app-monorepo",
  "private": true,
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "check-types": "turbo run check-types",
    "deploy": "wrangler deploy"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^2.6.0",
    "typescript": "5.9.2"
  },
  "packageManager": "bun@1.3.1",
  "dependencies": {
    "bun-plugin-tailwind": "^0.1.2"
  }
}
