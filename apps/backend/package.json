{
  "name": "backend",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "PORT=3001 bun run --hot index.ts",
    "build": "bun --bun build ./index.ts --outdir=dist --target bun --sourcemap=inline",
    "deploy": "wrangler deploy"
  },
  "private": true,
  "devDependencies": {
    "@types/bun": "^1.3.1",
    "wrangler": "^4.45.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@repo/types": "workspace:*",
    "hono": "^4.10.4"
  }
}
